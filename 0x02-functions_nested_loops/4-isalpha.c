#include <ctype.h>
#include "main.h"
/**
* main - check the code for ALX School students
* 
* Return: Always 0
*/
int main(void)
{
return (0);
}

int _isalpha(int c)
{
int res = isalpha(c);

if (res > 0)
{
return (1);
}
return (0);
}